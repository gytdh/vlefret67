apiVersion: v1
kind: Service
metadata:
  name: ysedrftt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ysedrftt"
    port: 8080
  selector:
    app: ysedrftt
